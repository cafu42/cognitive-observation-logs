{
  "version": "0.1.0",
  "description": "認知観察ログのスキーマ",
  
  "構造": {
    "プロフィール": {
      "ID": "string (例: child-01)",
      "タイプ": "string (例: 過集中_多動_完璧主義)"
    },
    "観察記録": {
      "type": "array",
      "items": {
        "年齢": "string (XyXm形式)",
        "観察日": "string (YYYY-MM-DD)",
        "状況": {
          "場所": "string",
          "活動": "string",
          "構造": "string (構造化/無構造/移行期)",
          "役割": "string"
        },
        "行動": {
          "パターン": "string",
          "継続時間_分": "number"
        },
        "結果": "string (調整維持/調整不全/部分的成功/離脱)",
        "推論": "string"
      }
    }
  },
  
  "備考": {
    "環境設計": "介入とその効果を記録したい場合は observationに追加可能"
  }
}
